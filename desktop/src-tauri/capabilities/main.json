{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "main",
  "description": "Permissions required by the main TACOS desktop window.",
  "windows": ["main"],
  "permissions": ["core:default", "dialog:allow-open"]
}
